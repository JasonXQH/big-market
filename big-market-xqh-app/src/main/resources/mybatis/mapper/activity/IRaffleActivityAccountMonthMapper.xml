<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.jasonxqh.infrastructure.dao.IRaffleActivityAccountMonthDao">
  <resultMap id="BaseResultMap" type="io.github.jasonxqh.infrastructure.dao.po.activity.RaffleActivityAccountMonth">

    <id column="id" jdbcType="INTEGER" property="id" />
    <result column="user_id" jdbcType="VARCHAR" property="userId" />
    <result column="activity_id" jdbcType="BIGINT" property="activityId" />
    <result column="month" jdbcType="VARCHAR" property="month" />
    <result column="month_count" jdbcType="INTEGER" property="monthCount" />
    <result column="month_count_surplus" jdbcType="INTEGER" property="monthCountSurplus" />
    <result column="create_time" jdbcType="TIMESTAMP" property="createTime" />
    <result column="update_time" jdbcType="TIMESTAMP" property="updateTime" />
  </resultMap>

  <insert id="insertActivityAccountMonth" parameterType="io.github.jasonxqh.infrastructure.dao.po.activity.RaffleActivityAccountMonth">
    insert into raffle_activity_account_month
    (user_id, activity_id,month, month_count, month_count_surplus, create_time, update_time)
    values
    (#{userId}, #{activityId}, #{month}, #{monthCount}, #{monthCountSurplus}, now(), now())
  </insert>

  <update id="updateActivityAccountMonthSubstractionQuota" parameterType="io.github.jasonxqh.infrastructure.dao.po.activity.RaffleActivityAccountMonth">
    update raffle_activity_account_month
    set update_time = now(),
        month_count_surplus = month_count_surplus - 1
    where activity_id = #{activityId} and user_id = #{userId} and month = #{month}
    and month_count_surplus > 0
  </update>

  <select id="queryActivityAccountMonthByUserId"
          parameterType="io.github.jasonxqh.infrastructure.dao.po.activity.RaffleActivityAccountMonth"
         resultMap="BaseResultMap"
  >
    select user_id, activity_id,month, month_count, month_count_surplus
    from raffle_activity_account_month
    where activity_id = #{activityId} and user_id = #{userId} and month = #{month}
  </select>

  <update id="updateActivityAccountMonthAddQuota">
    update raffle_activity_account_month
    set month_count_surplus = month_count_surplus + #{monthCountSurplus},
    month_count = month_count + #{monthCount},
    update_time = now()
    where user_id = #{userId} and activity_id = #{activityId} and month = #{month}
  </update>
</mapper>